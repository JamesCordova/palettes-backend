DEFAULT_PAGE_SIZE = 50
MAX_PAGE_SIZE = 200

HEX_CODE_PATTERN = r"^#[0-9A-Fa-f]{6}$"

# Cap on how many colors are embedded per palette in list/search responses.
# The single-palette detail endpoint always returns the full, uncapped set.
COLOR_PREVIEW_LIMIT = 20
